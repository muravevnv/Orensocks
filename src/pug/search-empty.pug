doctype html
html(lang="en")
  include includes/head
  +head('Корзина')
  body
    include mixins/_mixins
    include components/header
    main
      +breadcrumbs(['Главная', 'Каталог', 'Ничего не найдено'], 'Каталог')
      include blocks/search/empty

    include components/footer
    include includes/scripts